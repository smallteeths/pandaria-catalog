categories:
- Monitoring
questions:
- variable: defaultImage
  default: true
  description: "Use default Docker image"
  label: Use Default Image
  type: boolean
  show_subquestion_if: false
  group: "Container Images"
  subquestions:
  - variable: image.repository
    default: "dockerrrboy/webhook-receiver"
    description: "docker image name"
    type: string
    label: Image Name
  - variable: image.tag
    default: "latest"
    description: "docker image tag"
    type: string
    label: Image Tag
- variable: service.type
  default: "NodePort"
  description: "Service Type"
  label: webhook-receiver service Type
  type: enum
  options:
    - "ClusterIP"
    - "NodePort"
  required: true
  show_subquestion_if: "NodePort"
  group: Service
  subquestions:
  - variable: service.nodePort
    default: 30040
    description: "nodePort, only valid when serviceType is NodePort, between 30000-32767"
    label: node port
    type: int
    min: 30000
    max: 32767
- variable: config.providers.dingTalk.webhook_url
  default: https://oapi.dingtalk.com/robot/send?access_token=6267a918f5882cdde4853732914d8dbaf336f9249b2b1efbed05a9b029e3d4e0
  description: "providers Type"
  label: providers dingTalk webhook_url
  type: string
  required: true
  group: providers
- variable: config.providers.dingTalk.secret
  default: SEC9911e574dccb89e40bf4ca28de46e859b530605443646416e57e7e7e78906699
  description: "providers Type"
  label: providers dingTalk secret
  type: string
  required: true
  group: providers
- variable: config.receivers.test1.provider
  default: dingtalk
  description: "receivers Type"
  label: receivers dingTalk secret
  type: string
  required: true
  group: receivers


